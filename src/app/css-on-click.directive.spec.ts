import { CssOnClickDirective } from './css-on-click.directive';

describe('CssOnClickDirective', () => {
  it('should create an instance', () => {
    const directive = new CssOnClickDirective();
    expect(directive).toBeTruthy();
  });
});
